# Graph_model training environment
# Python >= 3.10 (3.11–3.13 recommended)
#
# Quick install (from repository root):
#   pip install -e .
#   # or:
#   pip install -r Graph_model/requirements.txt
#
# Conda (optional):
#   conda env create -f Graph_model/environment.yml
#   conda activate graph-model

# ── Core ML ─────────────────────────────────────────────────────────────────
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0

# ── PyTorch Geometric ────────────────────────────────────────────────────────
torch_geometric>=2.6.0

# ── Chemistry ────────────────────────────────────────────────────────────────
rdkit>=2024.3.1

# REQUIRED for Level-2 protein graphs (graph/level2_protein.py uses Bio.PDB).
# Without it ThreeLevelGraphBuilder catches the ImportError and substitutes a
# single all-zero residue node — so Option B, the only protein-aware model,
# silently trains on an empty receptor. This was missing from the file.
biopython>=1.83

# ── Data / numerics ──────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
scikit-learn>=1.5.0

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.66.0
matplotlib>=3.9.0
seaborn>=0.13.0
